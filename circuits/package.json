{
  "name": "@zk-digilocker/circuits",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "powershell -File ./scripts/build-circuits.ps1",
    "test": "mocha test/**/*.test.js",
    "clean": "rimraf build/*"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "snarkjs": "^0.7.0"
  },
  "devDependencies": {
    "chai": "^4.3.7",
    "mocha": "^10.2.0",
    "rimraf": "^5.0.1"
  }
}
