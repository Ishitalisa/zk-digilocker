{
  "name": "zk-digilocker",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "circuits",
    "contracts",
    "frontend",
    "subgraph"
  ],
  "scripts": {
    "prepare": "husky install",
    "build": "yarn workspaces run build",
    "test": "yarn workspaces run test"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "husky": "^8.0.0",
    "typescript": "^5.0.4"
  }
}