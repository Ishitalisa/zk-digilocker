{
  "name": "zk-digilocker",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "circuits",
    "contracts",
    "frontend",
    "subgraph"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "test": "yarn workspaces run test",
    "clean": "yarn workspaces run clean"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "typescript": "^5.0.4"
  }
}
